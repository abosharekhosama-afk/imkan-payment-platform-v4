# Books Connector

Internal connector writes `books_sync_state` as SYNCED for outbox-like events.

External Books system (Zoho or otherwise): **BLOCKED BY: DEC-016**.

API: `GET /api/v1/books/sync-state`
