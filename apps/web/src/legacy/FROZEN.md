# Frozen Legacy Frontend

**Status:** Feature-frozen (Phase 6.5 / DEC-014)

- Source: `main.legacy.tsx` — byte-preserved copy of the pre–Phase 6.5 `main.tsx` console.
- This directory is **not** part of the active V4 application.
- Do **not** import these modules into `src/v4/**` or the V4 entrypoint.
- Do **not** delete without explicit approval after V4 console acceptance.
- Active console uses `/api/v1` only; Legacy `/v1` + MySQL remain frozen on the backend.
